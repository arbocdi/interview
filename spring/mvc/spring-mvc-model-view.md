# Spring MVC — Model и View (кратко)

## Вопрос
Получает ли View модель, заполненную контроллером?

## Короткий ответ
**Да. View получает Model, заполненную контроллером, и использует её при рендеринге.**

---

## Как это работает пошагово

1. Spring **автоматически создаёт Model** (обычно `ModelMap`)
2. Контроллер **наполняет Model**
3. `DispatcherServlet` получает:
   - имя view
   - Model
4. `ViewResolver` находит конкретный View
5. **View рендерится на сервере**, используя данные из Model
6. Клиенту отправляется готовый HTML

---

## Пример

```java
@GetMapping("/user")
public String user(Model model) {
    model.addAttribute("name", "Alex");
    return "user";
}
```

```html
<p th:text="${name}"></p>
```

---

## Важные уточнения (ловушки)

- View **не изменяет** Model
- Model **не отправляется в браузер**
- Model существует **только на сервере**
- Браузер получает **уже отрендеренный HTML**

---

## Неявное добавление в Model

```java
@GetMapping("/user")
public User user() {
    return new User("Alex");
}
```

Если:
- используется `@Controller`
- **нет `@ResponseBody`**

👉 объект будет автоматически добавлен в Model  
👉 имя атрибута = `user`

---

## One-liner для собеседования

> Контроллер наполняет Model, DispatcherServlet передаёт её во View, а View использует данные модели при серверном рендеринге HTML.
